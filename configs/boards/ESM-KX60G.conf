
# ================================
# ESM-KX60G Board Configuration File
# ================================
CONFIG_BOARD_NAME="ESM-KX60G"

# ------------------------------------------------------------------
# STATUS: HARDWARE-VALIDATED (initial bench test 2026-07-07)
# Auto-ported from the legacy driver (Avalue_driver_deprecate/config).
#   WDT  - bench-tested OK (initial functional pass)
#   HWM  - bench-tested OK (initial functional pass); cross-module EC
#          index+data port contention fixed and stress-confirmed (#12):
#          fan1_input tach anomalies went from 19-27 per ~1200 reads to
#          0 per 1185 reads once the port pair was muxed across modules
#   GPIO - bench-tested OK: 4-pair jumper loopback (GPI0<->GPO4,
#          GPI1<->GPO5, GPI2<->GPO6, GPI3<->GPO7) passed on real
#          hardware (#11); needed a write/readback mutex fix in
#          src/hal/ec/ite_gpio.c (hal_gpio_lock), see GPIO section below
# WDT, HWM and GPIO have all now been bench-tested OK on real hardware,
# but the individual "# TODO verify" values below (CHIPID, voltage LSB,
# threm_* and other channels) are NOT yet metrologically confirmed
# against known rails - keep verifying each before relying on this board.
# ------------------------------------------------------------------

# ==================================
# ===== Watchdog Configuration =====
# ==================================
MAKE_WDT_DEVICE=ec
MAKE_WDT_CHIPSET=ite
CONFIG_WDT_CHIPSET=ite
CONFIG_CHIPID=0x5782
# Legacy stores chip type (it85x8), not a numeric ID. TODO verify CHIPID on hardware

# ==========================================
# ===== Hardware Monitor Configuration =====
# ==========================================
MAKE_HWM_DEVICE=ec
MAKE_HWM_CHIPSET=ite
CONFIG_HWM_CHIPSET=ite
# TODO verify CHIPID on hardware

# ===== Hardware Monitor Voltage Configuration =====
# legacy vin[]    = { nodev, sensor_vin, sensor_vcore, threm_cpu, threm_sys }
# legacy vin_vr[] = { vr_def, vr_200_17,  vr_def,       vr_def,    vr_def }
# ITE volt step = legacy 117/10 mV/count; derived from ec_vr_cal() in lib/core.c
# TODO verify LSB=12 on hardware against known rails

CONFIG_HWM_VOLTAGE_0_ENABLE=0

CONFIG_HWM_VOLTAGE_1_ENABLE=1
CONFIG_HWM_VOLTAGE_1_REG=ITE_REG_HWM_VOLTAGE(1)
CONFIG_HWM_VOLTAGE_1_LABEL="VIN"
CONFIG_HWM_VOLTAGE_1_LSB=12
# vr_200_17 -> r1=200k ohm, r2=17k ohm
CONFIG_HWM_VOLTAGE_1_R1=200000
CONFIG_HWM_VOLTAGE_1_R2=17000

CONFIG_HWM_VOLTAGE_2_ENABLE=1
CONFIG_HWM_VOLTAGE_2_REG=ITE_REG_HWM_VOLTAGE(2)
CONFIG_HWM_VOLTAGE_2_LABEL="VCORE"
CONFIG_HWM_VOLTAGE_2_LSB=12
# vr_def -> measured directly, no divider
CONFIG_HWM_VOLTAGE_2_R1=0
CONFIG_HWM_VOLTAGE_2_R2=0

# threm_cpu is a genuine EC voltage channel (not padding) - must be ENABLED per guide §2.
CONFIG_HWM_VOLTAGE_3_ENABLE=1
CONFIG_HWM_VOLTAGE_3_REG=ITE_REG_HWM_VOLTAGE(3)
CONFIG_HWM_VOLTAGE_3_LABEL="Threm_Cpu"
CONFIG_HWM_VOLTAGE_3_LSB=12
# vr_def -> measured directly, no divider
CONFIG_HWM_VOLTAGE_3_R1=0
CONFIG_HWM_VOLTAGE_3_R2=0

# threm_sys is a genuine EC voltage channel (not padding) - must be ENABLED per guide §2.
CONFIG_HWM_VOLTAGE_4_ENABLE=1
CONFIG_HWM_VOLTAGE_4_REG=ITE_REG_HWM_VOLTAGE(4)
CONFIG_HWM_VOLTAGE_4_LABEL="Threm_Sys"
CONFIG_HWM_VOLTAGE_4_LSB=12
# vr_def -> measured directly, no divider
CONFIG_HWM_VOLTAGE_4_R1=0
CONFIG_HWM_VOLTAGE_4_R2=0

# Fixed to 5 voltage sensors for ITE
CONFIG_HWM_VOLTAGE_NUM=5
CONFIG_HWM_VOLTAGE_MAP={ CONFIG_HWM_VOLTAGE_0_ENABLE, CONFIG_HWM_VOLTAGE_1_ENABLE, CONFIG_HWM_VOLTAGE_2_ENABLE, CONFIG_HWM_VOLTAGE_3_ENABLE, CONFIG_HWM_VOLTAGE_4_ENABLE }


# ===== Hardware Monitor Temperature Configuration =====
# legacy temp[] = { temp_cpu, temp_sys }
#define ITE_REG_HWM_TEMPERATURE(n) (0x3B + (n))

CONFIG_HWM_TEMPERATURE_0_ENABLE=1
CONFIG_HWM_TEMPERATURE_0_REG=ITE_REG_HWM_TEMPERATURE(0)
CONFIG_HWM_TEMPERATURE_0_LABEL="CPU"

CONFIG_HWM_TEMPERATURE_1_ENABLE=1
CONFIG_HWM_TEMPERATURE_1_REG=ITE_REG_HWM_TEMPERATURE(1)
CONFIG_HWM_TEMPERATURE_1_LABEL="System"

# legacy temp[] has no 3rd entry -> physical channel 2 unpopulated on this board
CONFIG_HWM_TEMPERATURE_2_ENABLE=0

# Fixed to 3 temperature sensors for ITE
CONFIG_HWM_TEMPERATURE_NUM=3
CONFIG_HWM_TEMPERATURE_MAP={ CONFIG_HWM_TEMPERATURE_0_ENABLE, CONFIG_HWM_TEMPERATURE_1_ENABLE, CONFIG_HWM_TEMPERATURE_2_ENABLE }

# ===== Hardware Monitor Fan Configuration =====
# legacy fan[] = { nodev, nodev, fan_cpu } -> CPU fan is physical channel 2 (Rule 1)
#define ITE_REG_HWM_FAN_SPEED(n) (0x35 + (n) * 2)

CONFIG_HWM_FAN_0_ENABLE=1
CONFIG_HWM_FAN_0_REG_SPEED=ITE_REG_HWM_FAN_SPEED(2)
CONFIG_HWM_FAN_0_LABEL="CPU Fan"
CONFIG_HWM_FAN_0_SPEED_STEP=1

CONFIG_HWM_FAN_1_ENABLE=0

# Fixed to 2 fan sensors for ITE
CONFIG_HWM_FAN_NUM=2
CONFIG_HWM_FAN_MAP={ CONFIG_HWM_FAN_0_ENABLE, CONFIG_HWM_FAN_1_ENABLE }

# ===== Hardware Monitor PWM Configuration =====
# legacy pwm[] = { backlight, fan_cpu }
#define ITE_REG_HWM_PWM(n) (0x40 + (n))

CONFIG_HWM_PWM_0_ENABLE=1
CONFIG_HWM_PWM_0_REG=ITE_REG_HWM_PWM(0)
CONFIG_HWM_PWM_0_LABEL="Backlight"

CONFIG_HWM_PWM_1_ENABLE=1
CONFIG_HWM_PWM_1_REG=ITE_REG_HWM_PWM(1)
CONFIG_HWM_PWM_1_LABEL="CPU Fan PWM"

CONFIG_HWM_PWM_2_ENABLE=0
CONFIG_HWM_PWM_3_ENABLE=0
CONFIG_HWM_PWM_4_ENABLE=0
CONFIG_HWM_PWM_5_ENABLE=0

CONFIG_HWM_PWM_NUM=6
CONFIG_HWM_PWM_MAP={ CONFIG_HWM_PWM_0_ENABLE, CONFIG_HWM_PWM_1_ENABLE, CONFIG_HWM_PWM_2_ENABLE, CONFIG_HWM_PWM_3_ENABLE, CONFIG_HWM_PWM_4_ENABLE, CONFIG_HWM_PWM_5_ENABLE }

# LVDS not supported — omitted (no LVDS HAL in this project). Legacy lvds[] = { lvds_fqy, lvds_mode, lvds_type }.
# Fan mode control not supported — omitted (no fan_mode HAL in this project). Legacy fan_mode[] = { fan_cpu }.
# ==============================
# ===== GPIO Configuration =====
# ==============================
# HARDWARE-VALIDATED: 4-pair jumper loopback (GPI0<->GPO4, GPI1<->GPO5,
# GPI2<->GPO6, GPI3<->GPO7) passed on real hardware, see #11. Sourced from
# the official ESM-KX60G EC BRAM map, not guessed: GPIO here is EC-native
# (through the on-board ITE it85x8 EC, read via its BRAM window), so it uses
# the ec/ite GPIO HAL with no MAKE_GPIO_PROTOCOL. The EC exposes a fixed
# split of 4 inputs and 4 outputs (no direction register):
#   BRAM 0x5A [3:0]  GPI  - 4 input lines
#   BRAM 0x5B [3:0]  GPO  - 4 output lines (write 1/0 = high/low)
# Model: fixed direction, separate input/output banks.
# A write to CONFIG_GPIO_REG_OUTPUT on this board needs the poll-for-readback
# retry in src/hal/ec/ite_gpio.c (hal_gpio_lock): this EC's own mirror of
# 0x5B can lag a write by up to ~110ms with no ready/busy bit to poll instead
# - a hardware fact to account for, not a driver bug left to fix further.
MAKE_GPIO_DEVICE=ec
MAKE_GPIO_CHIPSET=ite

# Fixed-direction, split banks: 0x5A = input, 0x5B = output
CONFIG_GPIO_DIR_FIXED=1
CONFIG_GPIO_REG_INPUT=0x5A
CONFIG_GPIO_REG_OUTPUT=0x5B
# Logical pins 4-7 are the outputs (bit per pin); 0-3 are inputs
CONFIG_GPIO_DIR_MASK=0xF0

# Pins 0-3: GPI bits 0-3 of 0x5A.  Pins 4-7: GPO bits 0-3 of 0x5B.
CONFIG_GPIO_PIN0_OFFSET=0
CONFIG_GPIO_PIN1_OFFSET=1
CONFIG_GPIO_PIN2_OFFSET=2
CONFIG_GPIO_PIN3_OFFSET=3
CONFIG_GPIO_PIN4_OFFSET=0
CONFIG_GPIO_PIN5_OFFSET=1
CONFIG_GPIO_PIN6_OFFSET=2
CONFIG_GPIO_PIN7_OFFSET=3
CONFIG_GPIO_PIN_NUM=8
CONFIG_GPIO_PIN_MAP={ CONFIG_GPIO_PIN0_OFFSET, CONFIG_GPIO_PIN1_OFFSET, CONFIG_GPIO_PIN2_OFFSET, CONFIG_GPIO_PIN3_OFFSET, CONFIG_GPIO_PIN4_OFFSET, CONFIG_GPIO_PIN5_OFFSET, CONFIG_GPIO_PIN6_OFFSET, CONFIG_GPIO_PIN7_OFFSET }
# ==============================
# ===== MISC Configuration =====
# ==============================
# BRAM 0x4B is EC firmware version, read-only, confirmed against the official
# ESM-KX60G_EC_BRAM_map.txt line 37 ("4B  R  EC firmware version") - a direct
# citation, not a guess, so it carries no "# TODO verify" tag. The
# "MISC omitted" note this section replaces was about the legacy 3.x
# esm_kx60g_init() never wiring a MISC channel table (config.c has no
# per-board MISC table to port from) - it does not mean this board has no
# ad-hoc register worth exposing.
MAKE_MISC_DEVICE=ec
MAKE_MISC_CHIPSET=ite

# ioctl magic (single-quoted char literal)
CONFIG_MISC_IOCTL_BASE='A'

CONFIG_MISC_0_ENABLE=1
CONFIG_MISC_0_REG=0x4B
CONFIG_MISC_0_MASK=0xFF
CONFIG_MISC_0_IOR=_IOR(CONFIG_MISC_IOCTL_BASE, CONFIG_MISC_0_REG, u32)
# Read-only per BRAM map ("R") - 0 makes hal_misc_write's __misc_store return -EACCES
CONFIG_MISC_0_IOW=0
CONFIG_MISC_0_LABEL="EC_FW_Version"

CONFIG_MISC_1_ENABLE=0
CONFIG_MISC_2_ENABLE=0
CONFIG_MISC_3_ENABLE=0
CONFIG_MISC_4_ENABLE=0
CONFIG_MISC_5_ENABLE=0
CONFIG_MISC_6_ENABLE=0
CONFIG_MISC_7_ENABLE=0
CONFIG_MISC_8_ENABLE=0
CONFIG_MISC_9_ENABLE=0

CONFIG_MISC_NUM=10
CONFIG_MISC_MAP={ CONFIG_MISC_0_ENABLE, CONFIG_MISC_1_ENABLE, CONFIG_MISC_2_ENABLE, CONFIG_MISC_3_ENABLE, CONFIG_MISC_4_ENABLE, CONFIG_MISC_5_ENABLE, CONFIG_MISC_6_ENABLE, CONFIG_MISC_7_ENABLE, CONFIG_MISC_8_ENABLE, CONFIG_MISC_9_ENABLE }
